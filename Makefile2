# *** MACROS
CC = cc

# *** TARGETS
engine: engine.o sandbox.o
	$(CC) -o engine engine.o sandbox.o -lSDL2 -lGL

engine.o: engine.c sandbox.h
	$(CC) -c engine.c

sandbox.o: sandbox.c sandbox.h
	$(CC) -c sandbox.c -lSDL2 -lGL

clean:
	rm -rf *.o
